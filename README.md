# Accredian Enterprise Website

## Setup Instructions

### Frontend
1. Install dependencies:
   ```bash
   npm install
   ```
2. Start the development server:
   ```bash
   npm run dev
   ```
3. Open the local URL shown by Vite (usually http://localhost:4000).

### Backend
1. Navigate to the Backend folder.
2. Install backend dependencies if needed:
   ```bash
   npm install
   ```
3. Set up your environment variables:
   - `DATABASE_URL` for the Supabase/Postgres connection string
4. Run the backend locally:
   ```bash
   node app.js
   ```

### Environment Variables
Create a `.env` file in the project root for frontend usage:
```env
VITE_BACKEND_URL=http://localhost:3000
```

For the backend, create a `.env` file inside the Backend folder:
```env
DATABASE_URL=your_supabase_connection_string
```

## Approach Taken
The primary approach was to understand the page, its content, and the available data and links, then recreate them into a sleek, modern website experience. The design was shaped as a polished enterprise landing page inspired by a modern boilerplate style, with a focus on clarity, visual hierarchy, and user experience.

The backend was designed completely by me to store enquiry form data securely and efficiently using Express and Node.js, with Supabase/Postgres as the database layer.

## AI Usage Explanation
Antigravity was used to help structure the website content, improve the overall layout and UI flow, The goal was to speed up development while keeping the project aligned with the original Website.

## Database Schema (Supabase)
```sql
create table public.users (
  id bigint generated by default as identity not null,
  created_at timestamp with time zone not null default now(),
  name text null,
  email text null,
  phone text null,
  ticket text null,
  domain text null,
  constraint users_pkey primary key (id)
) TABLESPACE pg_default;
```

## Improvements I Would Make With More Time
With more time, I would make the website even stronger by:
- adding a chatbot for visitor assistance
- creating a more animated and visually immersive experience
- improving the overall polish and responsiveness
- adding an admin section to manage tickets and enquiries
- enhancing the backend with better validation, logging, and analytics
